{
  "name": "curso-arquitecturas-distribuidas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run course:sync && node scripts/vuepress-dev.mjs",
    "course:sync": "node scripts/generate-code-pages.mjs",
    "build": "npm run course:sync && vuepress build docs",
    "docs:dev": "npm run course:sync && vuepress dev docs",
    "docs:build": "npm run build"
  },
  "devDependencies": {
    "@vuepress/bundler-vite": "2.0.0-rc.30",
    "@vuepress/theme-default": "2.0.0-rc.132",
    "sass": "1.101.0",
    "sass-embedded": "1.100.0",
    "sass-loader": "17.0.0",
    "vue": "3.5.42",
    "vuepress": "2.0.0-rc.30"
  }
}
